#include "main.h"
/**
* main - a program that prints alphabets
* Return: Always 0.
*/
int main(void)
{
print_alphabet();
return (0);
}

void print_alphabet(void)
{
char alph = 'a';
while (alph <= 'z')
{
_putchar(alph);
alph++;
}
}
